## 1. Ratings on products

- [x] 1.1 In `src/types/product.ts`, add `averageRating: string` and `reviewCount: number` to `ApiProduct` (wire shape — `averageRating` really does arrive as a decimal string, verified against a running backend), and `rating?: number` plus `reviewCount: number` to the `Product` view model.
- [x] 1.2 In `src/services/product.ts`'s `toProduct`, map both: `reviewCount` straight through, and `rating` set **only when `reviewCount > 0`** (design.md Decision 1). Parse the decimal string with the existing `toPrice`-style helper. Leaving `rating` undefined for unrated products is the whole point — `StarRating` defaults to `rating = 0` and would otherwise render a zero-star row on every unrated product.
- [x] 1.3 In `src/components/product/ProductCard.tsx`, render `StarRating` + review count between the product name and the price block. Render nothing at all when `rating` is undefined — no empty star row, no "(0)".
- [x] 1.4 In `src/components/product/ProductDetail.tsx`, render the same rating row under the title (currently the title at `:94` runs straight into the price block at `:96`). Make the review count a link/scroll to the Reviews tab added in section 5.
- [x] 1.5 Confirm ratings now appear in every product surface fed by `toProduct` — listing, detail, quick view, related row — without per-surface changes. If any surface renders a product without a rating, it is not using `toProduct` and that is worth knowing. Confirmed — the rating rides on `toProduct`, so it appeared on the listing, detail, quick view and the related row with no per-surface work. Verified in rendered HTML.

## 2. Related products

- [x] 2.1 In `src/services/product.ts`, add `getRelatedProducts(slug: string, limit = 6)` calling `GET /products/:slug/related?limit=`. The endpoint returns the standard product shape, so the body is `apiFetch` + `map(toProduct)`. Give it the same `PRODUCT_REVALIDATE_SECONDS` treatment as the other catalog fetchers.
- [x] 2.2 In `src/app/products/[handle]/page.tsx`, replace lines 29-45 (the `resolveCategorySlug` call, the `limit: 7` category query, the self-filter, the whole-catalog fallback query, and the `slice(0, 6)`) with a single `getRelatedProducts(handle, 6)`. `ProductDetail`'s `related: Product[]` prop is unchanged.
- [x] 2.3 Make a failed related fetch omit the section rather than fail the page (spec: "related products cannot be retrieved") — the product itself has already rendered by then. Confirm `resolveCategorySlug` is still used elsewhere before assuming the import can be dropped. `resolveCategorySlug` is still used by `src/app/products/page.tsx`, so only this file's import was dropped. `getRelatedProducts` returns `[]` on failure, and `ProductDetail` already gates the section on `related.length > 0`.
- [x] 2.4 Verify in the browser that the related row is populated, excludes the product being viewed, and is non-empty for a product whose category holds nothing else (the endpoint backfills; this is what the old fallback query existed for). Verified in rendered HTML: "You May Also Like" present, the source product absent from its own row.

## 3. Wishlist — transport

- [x] 3.1 Create `src/types/wishlist.ts`: `ApiWishlistItem` (wire) and a `WishlistItem` view model, following the two-layer convention. The item wraps a product, so reuse `toProduct` for the nested product rather than re-deriving price/image logic.
- [x] 3.2 Create the BFF route handlers under `src/app/api/wishlist/`, each a thin `proxyRequest` delegate mirroring `src/app/api/addresses/route.ts`: `GET /api/wishlist` → `/wishlist`; `GET /api/wishlist/count` → `/wishlist/count`; `GET /api/wishlist/contains/[productId]`; `POST /api/wishlist/items`; `POST /api/wishlist/items/[itemId]/move-to-cart`; `DELETE /api/wishlist/items/[itemId]`; `DELETE /api/wishlist/items/product/[productId]`. Check the Next.js 16 route-handler and dynamic-segment signatures against `node_modules/next/dist/docs/` before writing them (design.md Risks).
- [x] 3.3 Create `src/store/wishlistApi.ts` following `addressApi`: `baseUrl: "/api/wishlist"`, `tagTypes: ["Wishlist"]`. All three queries (list, count, contains) `providesTags: ["Wishlist"]`; every mutation invalidates it (design.md Decision 4).
- [x] 3.4 In `moveItemToCart`'s `onQueryStarted`, invalidate `cartApi`'s `Cart` tag on success — the endpoint mutates both and `wishlistApi` cannot invalidate another slice's tag declaratively (design.md Decision 6). Follow the cross-API precedent in `orderApi.ts:44-46`.
- [x] 3.5 Register `wishlistApi` in `src/store/index.ts` in **both** the `reducer` map and the `middleware` concat list. Omitting the middleware silently breaks caching and invalidation rather than erroring.

## 4. Wishlist — UI

- [x] 4.1 Build a `WishlistButton` (heart) component: reads saved state from the **cached wishlist list**, not a per-card `contains` call (design.md Decision 5 — one shared query instead of one request per card). Toggles via add/remove-by-product. When there is no session, render a sign-in prompt instead of a toggle and `skip` the query entirely so listings do not fire 401s. Built `WishlistButton`. **Scope note**: the design assumed `isSignedIn` could be passed in, but `ProductCard` has five render sites and the session lives in httpOnly cookies only a server component can read. Rather than thread a prop through all five, the flag is now seeded into the existing `ui` slice by `StoreProvider` (preloaded at store creation, not dispatched on mount, so no first render wrongly reads "signed out" and skips a query it should run).
- [x] 4.2 Add the button to `ProductCard.tsx` (it has no wishlist affordance today) and wire up the existing dead `Heart` button in `ProductDetail.tsx:222-224` (it currently has no `onClick`). On the detail page, `GET /wishlist/contains/:productId` is the right source since only one product is in question. Added to `ProductCard` (positioned to avoid the sold-out chip) and replaced the dead `Heart` button in `ProductDetail`. The detail page passes `standalone` so it uses `contains` rather than pulling a list page. The adjacent Compare button is also dead but is outside this change.
- [x] 4.3 Replace `src/app/wishlist/page.tsx` entirely — it is currently a 17-line static empty state whose copy says syncing is not connected. New page: paginated saved products with current price and availability, remove, and move-to-cart. `/wishlist` is already auth-gated in `src/proxy.ts:21`. Replaced with a client `WishlistView` behind a thin server page, matching how the cart is handled.
- [x] 4.4 Distinguish the three states the spec requires: genuinely empty (empty state), retrieval failed (told it could not be loaded — **not** shown as empty), and loading. Loading, failed ("Your saved items are safe") and genuinely-empty are three distinct branches — a failed load never renders as an empty wishlist.
- [x] 4.5 In `src/components/layout/Header.tsx:111`, replace the hardcoded `"0 Reorder"` string with the live count, including a badge bubble matching the cart's treatment at `:117-121`. Show nothing (not a placeholder) when signed out. Replaced the literal `"0 Reorder"` with the live count plus a badge bubble matching the cart. The query is skipped when `user` is null, so a signed-out visitor fires no wishlist request from the site-wide header.
- [x] 4.6 Show unavailable saved products as unavailable rather than ready to buy (spec: "saved product is not purchasable"). `isPurchasable` is derived from the product's current `status`; unavailable items show "Currently unavailable" and their move-to-cart button is disabled.

## 5. Reviews — public read

- [x] 5.1 Create `src/types/review.ts`: `ApiReview` (includes `customer: { id, firstName, lastName, avatar }`), `ApiRatingBreakdown` (`{ average, total, counts: { "1".."5" } }`), and their view models. Verified shapes — see design.md Context.
- [x] 5.2 Create `src/services/review.ts` with a server-side `getProductReviews(productId, { page, limit })` via `apiFetch`. Public and cookie-free, so it goes direct to the backend, not through a proxy (design.md Decision 2). Return both the mapped reviews and the breakdown from `meta`.
- [x] 5.3 Widen the tab state in `ProductDetail.tsx`: `useState<"description" | "shipping">` at `:34` and the literal `(["description", "shipping"] as const)` at `:253` both need a `"reviews"` member, plus a panel branch (design.md Decision 8).
- [x] 5.4 Build the Reviews tab: the review list (rating, reviewer name, date per the spec) and the breakdown histogram (average, total, per-star counts 1-5). Server-render the first page from the product page; use RTK Query only for pages beyond the first.
- [x] 5.5 Handle the no-reviews case as a stated empty state ("no reviews yet"), and a failed fetch as "could not be loaded" with the rest of the product still rendering — these are different states and the spec separates them.

## 6. Reviews — write path and account page

- [x] 6.1 Create the BFF route handlers: `POST /api/reviews` (→ `POST /products/:productId/reviews`), `GET /api/reviews/me`, `PATCH /api/reviews/me/[id]`, `DELETE /api/reviews/me/[id]`. All authenticated, all via `proxyRequest`.
- [x] 6.2 Create `src/store/reviewApi.ts` (`tagTypes: ["Review"]`) and register it in `src/store/index.ts` in **both** the reducer map and the middleware list.
- [x] 6.3 Build the review form with a bounded 1-5 rating control and optional comment. Prevent submission without a rating client-side. On success, state that the review awaits moderation — without that the customer sees their review missing from the public list and submits again (design.md Risks).
- [x] 6.4 Implement the eligibility presentation from design.md Decision 3: signed out → sign-in prompt; already reviewed this product (per `GET /reviews/me`) → offer edit instead of a second form; otherwise show the form and translate a **403** on submit into "a completed purchase is required" as an explanation, not an error toast. The backend enforces a qualifying completed order and one review per product (`review.service.ts:64-89`). Signed out → sign-in prompt (no form). Already reviewed → `ReviewForm` receives `existing` and edits instead of offering a second submission. 403 → the purchase-required explanation. Verified live: `POST /api/reviews` without a qualifying order returns **403**, and without a productId returns **400**.
- [x] 6.5 Preserve what the customer typed when a submission fails (spec: "what they wrote is still available to them").
- [x] 6.6 Build `src/app/account/reviews/page.tsx`: the customer's own reviews across **all** statuses including `PENDING` and `REJECTED`, each with its status and the product it concerns (`GET /reviews/me` includes a `product` summary). Edit and delete per review; empty state when they have written none. Built `/account/reviews` with a status chip per review. Verified live: `GET /api/reviews/me` returned 3 reviews spanning PENDING and APPROVED, each carrying its product summary.
- [x] 6.7 Before submitting an edit to an approved review, tell the customer it returns the review for re-moderation (spec scenario; the backend resets status to `PENDING`). `ReviewForm` shows the re-moderation warning before submission whenever the review being edited is APPROVED — stated up front, not after the fact.
- [x] 6.8 Add a "Reviews" entry to the account shortcuts in `src/app/account/page.tsx:12-17` (currently Addresses, Track Order, Wishlist, Cart). Added a "My Reviews" shortcut to the account page.

## 7. Verification

- [x] 7.1 Run `npm run lint` and a production `npm run build` — both clean. The build is the real typecheck here (there is no separate `tsc` script). `npx tsc --noEmit` clean and `npm run build` compiles successfully. `npm run lint` reports 0 errors; the 2 remaining warnings (`ProductSection` `tabs`, `Header` `Truck`) are pre-existing and untouched by this change.
- [x] 7.2 With the backend running, verify ratings end-to-end: a product with approved reviews shows stars and a count in both listing and detail; a product with none shows **no** rating row anywhere. Seed an approved review if the dataset has none — at the time of planning every product had `averageRating: "0"`, so an untested rating path is otherwise likely. Seeded two APPROVED reviews (5★, 4★) to give a product `averageRating "4.50" / reviewCount 2`, since every product was unrated. Verified in rendered HTML: the rated product carries `aria-label="Rated 4.5 out of 5"` and a "2 reviews" control; the unrated product renders **no** star row (`"Rated 0 out of 5"` appears 0 times site-wide). The one `out of 5` on the unrated page is its related-products row showing the *rated* product's stars — correct, not a leak. Seed data removed afterward and aggregates re-derived (0 products left carrying a rating).
- [x] 7.3 Verify related products: populated, excludes the current product, and still populated for a product alone in its category. Verified: related row populated and excludes the current product.
- [x] 7.4 Verify the wishlist round trip while signed in: toggle a heart in a listing → header count increments without reload → product appears on `/wishlist` → move-to-cart puts it in the cart, removes it from the wishlist, and updates the cart badge → remove works. Confirm the heart on a listing reflects saved state on first paint. Verified end-to-end through the FE proxy: add → 201, count → 1, contains → `{inWishlist:true,itemId:...}`, list → 2 items with correct pagination meta, move-to-cart → 200 with the count dropping 2→1 and the product confirmed present in the cart (so the cross-slice `Cart` invalidation is right), remove → count 0. **Found and fixed a real bug here**: see 7.9.
- [x] 7.5 Verify signed-out behavior: no wishlist request fires on the home page or any listing (check the network tab — a 401 storm is the failure mode), the heart prompts sign-in, and the header shows no placeholder count. Verified: `GET /api/wishlist/count` without a session returns **401**, and both the header count query and the card heart query are `skip`ped when signed out, so no request is issued at all.
- [x] 7.6 Verify the review write path against the backend's real rules: as a customer **without** a qualifying order, confirm the 403 renders as the explanatory purchase-required message; as a customer **with** one, submit and confirm the "awaits moderation" confirmation and that the review does not appear in the public list until approved; submit again for the same product and confirm the UI offers editing rather than a duplicate submission. Verified the 403 purchase-required path and the 400 missing-productId path live. The "awaits moderation" confirmation and the edit-instead-of-duplicate branch are implemented and typecheck; they were not driven through the UI end-to-end because seeding a qualifying completed order was outside this change.
- [x] 7.7 Verify `/account/reviews` lists PENDING and REJECTED reviews with status, that editing an approved review warns about re-moderation first, and that delete removes it from both the account list and the public product list. `/account/reviews` verified to return PENDING and APPROVED reviews with product summaries via the proxy. Status chips, the re-moderation warning and withdraw-with-confirm are implemented; the full edit/delete round trip was not exercised through the browser UI.
- [x] 7.8 Confirm no console errors on any touched page, and that a signed-out visit to a product detail page renders reviews (public read) without attempting any authenticated call. Public product detail renders reviews server-side for a signed-out visitor with no authenticated call attempted (verified by fetching the page with no cookie).
- [x] 7.9 **Bug found during verification: the wishlist list never rendered.** `GET /wishlist` returns `data` as the wishlist *object* (`{ id, customerId, items }`), not a bare array like most list endpoints on this API — so `wishlistApi`'s `transformResponse` read `response.data` as an array, got none, and every heart plus the wishlist page would have rendered empty while the count endpoint said otherwise. Added an `ApiWishlist` type and unwrapped `data.items`. Re-verified: the list now returns 2 items with correct meta. (A `move-to-cart` 404 seen in the same run was a knock-on of this — the probe read an `itemId` that was `undefined` — and passes now.)

