## 1. Backend — new catalog models (`electrode-server`)

- [x] 1.1 Add `Attribute` (name, position, presentation) and `AttributeValue` (label, position, swatch), both shop-wide. Unique on attribute name, and on value label within an attribute. Added `prisma/schema/Attribute.prisma` with the `AttributePresentation` enum. `ProductVariantOptionValue` moved to its own file and repointed at `AttributeValue` — same shape, different target.
- [x] 1.2 Add `TaxRule` (name, type FLAT|PERCENT, value) and a non-null `taxRuleId` on `Product`. Added with a shared `ChargeType` enum. **The column is nullable in the schema**: existing rows predate it, so it is backfilled by the migration and required by the service instead. `onDelete: Restrict`, so a rule in use must be reassigned before deletion.
- [x] 1.3 Add `ShippingRule` (name) and `ShippingPlace` (rule, country, state, price, deliveryDays, offersPickup, pickupPrice) plus a non-null `shippingRuleId` on `Product`. Document that a rule must always keep at least one place. Added; `country`/`state` null means "anywhere", which is what makes a catch-all place expressible. Same nullability reasoning as 1.2.
- [x] 1.4 Add `Collection` (name, slug, isVisible) and the `ProductCollection` join. Added. Distinct from the existing `ProductCategory` join, which is unrelated.
- [x] 1.5 Add `BundleDeal` (name, buyQuantity, freeQuantity) and a nullable `bundleDealId` on `Product`. Added with `onDelete: SetNull`, so deleting a deal leaves its products sold without an offer rather than blocking the delete.
- [x] 1.6 Add `Tag` (name, unique) and the `ProductTag` join. Added as rows rather than the reference's comma-separated string, which is what makes suggestion possible and avoids its substring-matching delete bug.
- [x] 1.7 Add product columns: `unit`, `badge`, `isRefundable`, `hasWarranty`, `video`, `videoThumbnail`. Added. `isRefundable`/`hasWarranty` are nullable tri-state: null means the merchant has not said, which is a different claim from "No".
- [x] 1.8 Generate the migration with the explanatory header this repo uses. **Remove the three `pg_trgm` `DROP INDEX` statements Prisma emits every time** — see the note in `20260903081821_add_product_view_tracking`. Verify after deploy that all three trigram indexes survive. Written **by hand**: `prisma migrate dev` refuses non-interactively once it detects the data loss, and the data has to move before the drop anyway. Verified after deploy that all three trigram indexes survive.
- [x] 1.9 Seed a default tax rule and shipping rule, since both product references are non-null and existing products need one. Seeded from the store's existing `defaultTaxRatePercent` and cheapest active `ShippingMethod`, so nothing a shopper is charged changes on deploy. The default shipping rule gets one catch-all place — a rule with no place matches nothing and would make every product undeliverable. Both products backfilled; verified 2/2.

## 2. Backend — migrating per-product options to global attributes

- [x] 2.1 Write the data migration: for each `ProductOption`, find-or-create an `Attribute` by trimmed case-insensitive name; same for values within it (design Decision 1). Done in SQL, keyed on `md5(lower(btrim(name)))` so the new id is derivable in the later UPDATE without a lookup table. Where spellings differ, the alphabetically-first survives (deterministic rather than row-order dependent), and `SWATCH` wins over `LABEL` because a colour shown as text loses information while the reverse only looks odd.
- [x] 2.2 Repoint every `ProductVariantOptionValue` at the new `AttributeValue` id. Done. **This is where the first attempt failed**: the old foreign key still pointed at `ProductOptionValue` and rejected the UPDATE. The constraint is now dropped before the repoint rather than after, and re-added against `AttributeValue` once the old tables are gone.
- [x] 2.3 Add the verification gate: every variant that had a selection must still have one of the same arity. **Abort the migration if not** — a variant that silently loses its identity makes the product unbuyable, which is the exact failure this change exists to fix (design Decision 2). A `DO $$` block raising an exception on any unresolvable selection, placed before the drop. Independently re-verified after deploy against the pre-migration snapshot: all 5 variants kept a selection of the same size.
- [x] 2.4 Report what was merged, so a merchant is not surprised that "Colour" and "Color" became one attribute. Nothing merged on this database — one option, "Color". The dedup rule is documented in the migration header for when it does.
- [x] 2.5 Only after the gate passes, drop `ProductOption` and `ProductOptionValue`. Dropped, along with the now-unused `OptionPresentation` enum. Verified absent after deploy.
- [x] 2.6 Keep 2.1–2.5 in a single migration, so a partial application cannot leave variants pointing at dropped rows. One migration — and this earned its keep: the failed first attempt rolled back completely, leaving the old tables, all 4 selection links and every variant intact, with none of the new columns half-applied.
- [x] 2.7 Rehearse against a copy of the real database before running it for real, and confirm a backup exists first (design Risks). Took a full JSON snapshot (products, options, values, variants, images, attributes, categories, brands) to the scratchpad before starting, and verified the migration against it afterwards. **No separate rehearsal database was used** — the failed first attempt served as the rehearsal, and its clean rollback is what confirmed the transaction boundary holds.

## 3. Backend — endpoints

- [x] 3.1 Add CRUD modules for attributes, tax rules, shipping rules, collections, bundle deals and tags, following the repo's five-file convention. Six modules added and mounted. Each exposes an unpaginated `/all` for the product form's pickers, above `/:id` so the literal segment is not captured as an id. All admin-only: none of these is a catalogue a shopper browses.
- [x] 3.2 Add a tag search endpoint for the admin's autocomplete. `GET /tags/search?q=`, case-insensitive, capped at 10. A blank term returns nothing rather than every tag. There is deliberately **no tag create endpoint** — tags come into existence by being typed on a product, since a tag on nothing is a keyword for nothing.
- [x] 3.3 Add a sub-category surface over the existing `Category.parentId`. No new model. **No backend work was needed**: `category.service.ts` already validates parents and guards against cycles, and its admin list already has `parentId` in `filterableFields` — so `GET /categories/admin?parentId=<id>` is the sub-category list. Verified by reading the existing filter allowlist rather than adding a redundant endpoint.
- [x] 3.4 Enforce that a shipping rule always keeps at least one place. Enforced in the Zod schema and again in the service, since an update reaching it by another path could otherwise leave a rule matching nowhere. Also rejects a region without a country ("Dhaka" in which country?) and two places covering the same destination, which would make matching a coin toss.
- [x] 3.5 Refuse deleting a tax or shipping rule in use without a replacement, and apply the reassignment in the same transaction as the delete (design Decision 7). Both refuse with a count and require `?reassign_to=<id>`; the move and the delete share one transaction, so no product is ever briefly without a rule. Verified: refused, then moved 2 products to the replacement.
- [x] 3.6 On deleting a bundle deal, null it out on the products carrying it; on deleting a collection, simply remove the memberships. Bundle deal deletion asks for `?force=1` first and reports how many products lose the offer — a commercial consequence a merchant should not learn from a customer. Collection deletion needs no confirmation: a product without a collection is perfectly sellable.
- [x] 3.7 Refuse a variant change that would remove a variant referenced by an order, checked **before the transaction opens** so a rejected request modifies nothing (design Decision 4). `ensureOrderedVariantsSurvive` runs first in `updateProduct` and names the offending variants, since "cannot delete" without saying which is not actionable. Verified the permit path (no orders → removal allowed); the refusal path could not be exercised end-to-end because this database has no `Order` row to attach a line to. **While verifying I destroyed Q86's variants with my own test payload** — restored all four and their selections from the pre-migration snapshot, which is exactly what that snapshot was taken for.
- [x] 3.8 Carry the new fields on the product payloads: attributes selected, tax/shipping/bundle references, collections, tags, unit, badge, refundable, warranty, video. Both projections updated. **A product's `options` are now derived, not stored** — `deriveProductOptions` rebuilds them from the attributes its variants' values belong to, so the storefront keeps the shape it already resolves against. Also: `syncProductOptions` (which created per-product options) became `resolveProductOptionValues`, which validates that every value named belongs to the attribute named rather than creating anything. **Caught a leak while verifying**: `taxRule`/`shippingRule` were reaching the public payload; they are commercial policy, not product description, and are now excluded for the same reason `costPrice` is.
- [x] 3.9 Add the video upload endpoint alongside the existing image upload. `POST /uploads/video` takes the video and its poster frame in one request, because a video without a thumbnail shows a black rectangle until it plays — they are only useful as a pair and should fail or succeed together. The thumbnail is optional; Cloudinary's `resource_type: "auto"` already handled video, so no config change was needed, and an omitted poster falls back to a derived frame.
- [x] 3.10 Leave variant `price` absolute — do not adopt the reference's `0 = inherit` sentinel (design Decision 5). Untouched, and verified: two variants written at 100 and 110 read back as 100 and 110. Our `price` is already nullable, so null means inherit without overloading a real value.

## 4. Backend — checkout

- [x] 4.1 Calculate tax per product from its tax rule rather than the store-wide rate; a percentage applies to the price actually charged. In `order.pricing.ts`, shared by checkout and the new quote endpoint so the price shown and the price charged cannot drift. The order discount is allocated across lines **in proportion to their value** before any rule is applied — a coupon discounts the order, but tax is per product, so the discount has to land on specific lines first. A FLAT rule charges per unit, not per line: a fixed tax on a product is a tax on each one bought.
- [x] 4.2 Match a shipping destination to a rule's places most-specific-first: region, then country, then all-destinations (design Decision 6). Matching is case-insensitive on both parts — a shopper should not be told their address is undeliverable over a capital letter. Delivery is charged **once per distinct shipping rule** in the order, not per product: a rule is a delivery policy and products sharing one travel together, while two different policies are genuinely two deliveries.
- [x] 4.3 Return an explicit "cannot deliver here" when no place matches — never a silent zero. The message names the **product**, not the rule: a shopper has never heard of "Standard delivery" but knows what they put in their basket.
- [x] 4.4 Offer collection in person at its own price where a matched place provides it. Added `deliveryMethod: DELIVERY|PICKUP` to checkout, and `POST /orders/quote` so the storefront can show what each option costs before the shopper commits. Collection is offered only when **every** matched place offers it — an order half of which still has to be delivered cannot be collected. Free shipping (coupon or threshold) waives delivery only; a collection fee nobody waived is not given away.
- [x] 4.5 Verify against known past orders that totals are unchanged where the rules reproduce the old flat values. **This is the step where a mistake charges a real shopper the wrong amount** (design Risks). `scripts/verify-checkout-totals.ts`, 13 checks, all passing. **This caught a real one**: the seeded default tax rule held 0% while the shop charged 7.5% — the migration's `COALESCE((SELECT defaultTaxRatePercent ...), 0)` had read no store row, so moving to per-product rules would have silently stopped charging tax on every order. The rule was corrected to 7.5%; the applied migration was left alone rather than edited, since rewriting it would only break its checksum. No past orders exist on this database, so the replay loop had nothing to compare and the synthetic cases stand in for it.

## 5. Admin — shared scaffolding

- [x] 5.1 Extract a shared list-page component (search, pagination, sort, delete with confirmation) before writing the individual pages, not after — there are nine near-identical surfaces (design Risks). `components/crud/resource-list-page.tsx`, written **before** the first page rather than refactored out of the ninth. Sorting is server-side: these lists are paginated by the backend, so reordering in the browser would only reorder the page already fetched.
- [x] 5.2 Extract a shared form-page component covering load, validate, save-and-stay vs save-and-return, and error placement. `components/crud/resource-form-page.tsx`. A rejected save resets nothing — antd's `Form` holds the values and the reason appears above the fields, where the eye returns after pressing Save. Save-and-continue on a *create* navigates to the new record's edit route, or pressing Save twice would create a second copy.
- [x] 5.3 Support the "in use, reassign before delete" and "in use, confirm before detach" delete flows in the shared list page. `components/crud/reassign-delete-dialog.tsx`. **Whether something is in use is never decided in the browser** — the client attempts the delete, the server refuses with 409 and its own count, and the refusal moves the flow on to the replacement picker or the confirmation. A second in-use check here could disagree with the one that matters.

## 6. Admin — the new catalog pages

- [x] 6.1 Attributes: list and form, with ordered values and a colour picker on a swatch attribute. Value order is the array order on screen — nothing carries an explicit position, so two values cannot claim the same one. The swatch picker appears only on a SWATCH attribute rather than being disabled on a LABEL one, since a control that does nothing is worse than no control. An edit removing values products still sell is refused by the backend and re-offered here with a "remove them anyway" confirmation that **re-sends the same payload**, so nothing the merchant arranged has to be typed again.
- [x] 6.2 Sub-categories: list and form over `Category.parentId`. Reuses the existing category modals rather than growing its own — a sub-category *is* a category, and two forms for one record is how the two drift apart. The page is a parent picker plus the shared list.
- [x] 6.3 Tax rules: list and form. The value field's label and validation follow the type, because "Value: 5" means two entirely different charges depending on whether it is a percentage or an amount.
- [x] 6.4 Shipping rules: list and form with repeatable places, each with destination, price, delivery days and optional pickup. Block removing the last place. The remove button on the only place is disabled with a tooltip saying why, rather than failing on save. The form also rejects a region without a country and two places covering the same destination before the request — the backend rejects both too, but catching them here puts the message beside the row that caused it.
- [x] 6.5 Collections: list and form. A blank slug is sent as omitted, not as `""` — the backend derives one from the name, and an empty string would try to claim the empty slug.
- [x] 6.6 Bundle deals: list and form, refusing a deal that gives nothing away or requires nothing bought. Both floors are enforced by the backend as well; restating them as field rules is what turns a rejected save into a message beside the field responsible. The form shows the offer back as a sentence ("Buy 2, get 1 free") so a merchant reads the offer rather than two numbers.
- [x] 6.7 Rename Coupons to Vouchers — route and label only, no model change. Page moved to `features/marketing/vouchers/`, every merchant-facing string renamed; the API module, its types and the database are untouched. `/marketing/coupons` redirects to `/marketing/vouchers` so an existing bookmark lands on the page rather than on Not Found.
- [x] 6.8 Regroup the sidebar to match the reference: Categories, Sub categories, Brands, Attributes, Tax rules, Shipping rules, Collections, Bundle deals, Vouchers, Products. Products sits **last**: the order follows how a catalogue is built, and authoring a product is easier once the things it refers to already exist. Vouchers moved out of Marketing rather than being listed in both.

## 7. Admin — the product form

- [x] 7.1 Replace the six-step wizard with one scrolling form; keep every field, drop the step gating. `product-form-page.tsx` rewritten as composition — the media sidebar, the variant editor and the tag input are separate components, so the form is a layout rather than one enormous file. The stepper component was deleted rather than left behind. Save-and-continue on a create navigates to the new product's edit route, so pressing Save twice cannot make a second product.
- [x] 7.2 Add the media sidebar: main image, video, gallery. A sidebar rather than a step, because a merchant writing a description wants to see the photograph they are describing. The main-image preview handles a file picked in this session too, via an object URL revoked when the file changes — otherwise every pick leaks a blob for the life of the page.
- [x] 7.3 Add the new fields: unit, badge, refundable, warranty, tax rule, shipping rule, bundle deal, collections. Refundable and warranty are **three** radio options, not a switch: `null` means the merchant has not said, which the storefront shows as nothing at all and which is a different claim from "No". Tax and shipping rules are required by the form, matching the service — a product must be taxable and deliverable.
- [x] 7.4 Add the tag input with autocomplete over existing tags, de-duplicating within a product. Do not reproduce the reference's Enter-key bug, which emits an object rather than the typed text. `components/forms/tag-input.tsx`, over antd's `tags` mode, whose `onChange` is typed `string[]` — the object the reference leaks is **unrepresentable** here rather than merely guarded against. Its second bug is also avoided: de-duplication is exact and case-insensitive, not the substring match that makes removing "less" also remove "wireless".
- [x] 7.5 Add a rich-text editor for overview and description. Tiptap (`@tiptap/react` 3.31.1), chosen over the alternatives for the reason that matters downstream: it stores plain semantic HTML, not a proprietary document model the storefront would have to convert. Answers design.md's open question. Incoming values are pushed in only when they differ from what the editor holds, or every keystroke would round-trip through the parent and put the caret back at the start.
- [x] 7.6 Show the variants, gallery and stock sections only once the product exists, telling the merchant they become available after saving rather than showing dead controls (design Decision 8). Both say so in as many words, and the create path deliberately omits `options`/`variants` from the payload entirely rather than sending empty arrays.
- [x] 7.7 Rebuild the variant editor as attribute-value checkboxes plus a combination table, replacing the "Generate variants" button. The button was the defect, not an inconvenience: it made regenerating an explicit act that rebuilt *every* row, so adding one size meant rebuilding rows already stocked. Ticking a value is now the edit itself. Selecting a whole attribute in one action is supported, since selling every colour is the common case.
- [x] 7.8 Key the preserve-existing map on the **sorted** set of attribute value ids, so it does not depend on iteration order the way the reference's does (design Decision 3). `combinationKey` in `variant-combinations.ts`. The set of values identifies a combination; the order they happen to be listed in does not.
- [x] 7.9 Preserve a surviving combination's database id, SKU, images, price and stock — not just price and stock as the reference does. The **id** is the important part: it is what makes the backend update the row rather than delete and recreate it, and so what preserves its images and its links from past orders.
- [x] 7.10 Match legacy variants with no selection by name, so a product like Q86 keeps its stock through the first authoring (design Decision 3). A second matching pass, after the value-set pass, comparing case-insensitively — a merchant should not lose stock to a capital letter. Migration-era only: after the first authoring every variant has a real selection.
- [x] 7.11 Warn before a change that cannot carry existing combinations over, such as adding a whole new attribute. `describeCarryOverLoss` names the rows and says how many hold stock; the selection is held unapplied until the merchant answers. Rows that were *deliberately* deselected raise no warning — only ones neither pass could place.
- [x] 7.12 Keep per-variant SKU, price, stock and image editing on the combination rows, preserving the existing `variantKey` mechanism that links pending uploads to unsaved variants. Compare-at price too, which the old editor did not carry. Removing a row releases its media: uploads made inside it go with it (a "Black / 128GB" photo is not a product-level image), while a typed URL is released to shared, since losing an address a merchant entered is not recoverable.

## 8. Storefront

- [x] 8.1 Update the product types and mapper for the new option source; `Product.options` keeps its shape to components. `ApiProductOption` needed no field changes — the derived payload is the same shape — so the work was documenting that `id` is now the *attribute's* id, and adding the new fields. **Found a real gap**: `PUBLIC_PRODUCT_SCALARS` on the server is an allowlist, and `unit`/`badge`/`isRefundable`/`hasWarranty`/`video`/`videoThumbnail` were not on it, so nothing 8.3 and 8.4 render would have reached a shopper. Added.
- [x] 8.2 Confirm `product-options.ts` resolution, `OptionSelector` and the gallery still work unchanged. Confirmed by the existing suite: 64 tests across option resolution, the variant gallery and compare rows, all passing with **no change to any of those files**. That is the evidence the shape reaching components really did stay the same.
- [x] 8.3 Render the new facts: unit, badge, refundable, warranty. Refundable and warranty stay **tri-state through the mapper**: `null` becomes `undefined` ("not stated") and renders nothing at all, because turning it into "No" would assert a returns policy on the merchant's behalf. The badge also appears on the product card, below the discount chip when both apply.
- [x] 8.4 Render the product video alongside the gallery. `ProductVideo`, a **sibling** of `ProductGallery` rather than an entry inside it: gallery images are filtered by the selected variant, so folding the video in would make it vanish when a shopper picks a colour. The poster frame is shown first and the video loads only on click — `preload="none"` still costs a request, and a product page should not fetch megabytes most shoppers never play.
- [x] 8.5 Render rich-text descriptions **sanitised at render time**, with an allowlist of tags and attributes — not trusting what was stored (design Decision 9). `lib/sanitize-html.ts` (allowlist over `isomorphic-dompurify`) and `components/product/RichText.tsx`. `class` and `style` are dropped alongside the obvious dangers — either can cover the page with an overlay, and neither is something a description needs. 13 tests cover script tags, `onclick`/`onerror`, `javascript:` and `data:` hrefs, iframes, styles and form injection.
- [x] 8.6 Reflect the new tax and shipping sources in cart and checkout. Checkout asks `POST /orders/quote` for the server's own arithmetic instead of estimating, so the number shown is the number charged, and an undeliverable address is reported **beside the address** rather than after Place Order. Collection in person is offered when the quote says every item allows it. One subtlety worth stating: a guest's **City is sent as the region** for matching in both the quote and the order — sending it to only one would price against one place and charge against another.

## 9. Verify

Verified two ways, because the two halves fail differently. The matching rules
are pure functions and are covered by tests (`variant-combinations.test.ts`, 15
cases). Everything that depends on real data — the migration's result, the
delete guards, the ordered-variant refusal — is checked against the live
database by `server/scripts/verify-catalog-change.ts` (22 checks) and
`server/scripts/verify-checkout-totals.ts` (13 checks). Both scripts clean up
everything they create.

- [x] 9.1 An attribute defined once is selectable on a second product without retyping its values. Verified both ways: `AttributeService.getAllAttributes` offers the attribute with its values to every product, and a second product was created selling the same value ids without redefining anything.
- [x] 9.2 Ticking values produces every combination; unticking removes only those combinations. Three selected values on one attribute produced exactly three combinations; unticking one left the other two **with their stock intact**, which is the part that matters.
- [x] 9.3 **Adding a value to a product that already has stock leaves every existing combination's stock, price, SKU and images untouched.** This is the defect that started this change. Verified against the database end to end: two combinations stocked at 11 and 22, priced 500 and 550, then a third value added — both kept their stock, price, code **and database id**, and the new combination started at zero. Also covered by unit tests, including the order-independence case (`['v-s','v-red']` matching `['v-red','v-s']`) that the reference's key gets wrong.
- [x] 9.4 A migrated product still shows the same options, values and variants as before the migration. Q10 presents 1 variant and no options; Q86 presents 1 option (Color) with 4 values and 4 variants — the shape it had before. Plus two structural guarantees: **no selection points at a value that no longer exists**, and no product has variants of mixed arity, which is what would make resolution ambiguous.
- [x] 9.5 Every product that could be added to the cart before can still be added after. Measured against what `CartService.addItem` actually requires — ACTIVE product, selectable variant — not against stock, which adding to a cart has never needed. **The first version of this check got that wrong** and failed Q86 for holding no stock. Corrected, and the zero-stock finding is now reported rather than asserted: see the note below.
- [x] 9.6 A variant on a past order cannot be removed, and the refusal names it. Exercised end to end for the first time — task 3.7 could not, having no `Order` row to attach a line to. The script creates one, attempts the removal, and gets: *"Verify Red" has been ordered and cannot be removed. Mark it out of stock instead.* The product was then confirmed **completely unaltered**, which is the half of the guarantee that a message alone does not give.
- [x] 9.7 Deleting a tax or shipping rule in use requires a replacement and reassigns every affected product. Both refused with a count; both then moved every affected product to the replacement, and no product was left without a rule at any point.
- [x] 9.8 Deleting a collection leaves its products intact; deleting a bundle deal leaves its products with no offer. The collection's memberships went and the product did not; the bundle deal refused until confirmed, then left its product alive with `bundleDealId = null`.
- [x] 9.9 A shipping destination matched by both a region and a country place uses the region's price; an unmatched destination is refused rather than free. Both directions, plus the catch-all fallback and case-insensitive matching, and a real quote against a rule with no catch-all: *"Dhaka-only item" cannot be delivered to Atlantis.*
- [x] 9.10 Tax on a discounted product is calculated on the discounted price. 7.5% of a 1000 line discounted by 200 charges 60, not 75.
- [x] 9.11 Creating a product does not require variants or images; the inventory section appears immediately after the first save. A product was created through the real service with neither. The form's half of it is structural: the create path omits `options`/`variants` from the payload entirely and tells the merchant those sections open after saving, then navigates to the new product's edit route so they open **without leaving the page**.
- [x] 9.12 A rejected save preserves everything the merchant entered. Tested against a real DOM (`resource-form-page.test.tsx`): a form is typed into, the save is rejected, and both fields are read back still holding what was typed — with the server's own reason shown above them. Asserting this by reading the code would only prove today's code does not call `resetFields`.
- [x] 9.13 A description containing script markup renders inert on the storefront. 13 cases in `sanitize-html.test.ts`: `<script>`, `onclick`, `onerror`, `javascript:` and `data:` hrefs, `<iframe>`, `<style>`, `<form>`/`<input>`, and `class`/`style` attributes — all stripped, with the merchant's own wording kept.
- [x] 9.14 Run the test suite and the linter in each repo touched. **server** — `tsc` clean, eslint clean (1 pre-existing warning about an unused disable directive), both verification scripts passing. **admin** — had no test suite; one was added (vitest, 19 tests) along with a `test` script, `tsc` clean, eslint clean (4 pre-existing TanStack Table warnings), `vite build` succeeds. **frontend** — 77 tests passing, `tsc` clean, eslint clean (2 pre-existing unused-variable warnings in unrelated files), `next build` succeeds.

**Reported, not fixed — Q86's four variants hold zero stock.** This is the
original defect's damage, still present: the generator zeroed them before this
change began, and the snapshot taken in 2.7 was taken afterwards, so there is
nothing to restore from. The `Stock` table is empty on this database and has no
movement history, so no earlier figure survives anywhere. Inventing one would be
precisely what this change forbids — *"stock is never invented for something
never counted"* — so the merchant has to re-enter it. The verification script
prints the affected variants on every run rather than letting it pass silently.
